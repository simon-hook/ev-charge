# Vendored Audi cloud client extracted from the Home Assistant audi_connect_ha integration.
# This package is intentionally empty except for this marker so that the copied modules
# (audi_connect_account.py, audi_services.py, audi_api.py, audi_models.py, util.py, const.py)
# can resolve their relative imports (e.g. `from .audi_api import AudiAPI`).
#
# The .py client files are NOT committed here by default — run the one-time copy step in
# README.md to drop them in.
