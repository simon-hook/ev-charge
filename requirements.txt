# Core runtime dependencies. These always install cleanly.
requests>=2.31
python-dotenv>=1.0
aiohttp>=3.9
beautifulsoup4>=4.12   # required by the vendored Audi client (ev_charge/vendor/audiconnect/)

# Dev / tests
pytest>=8.0

# NOTE: the Audi cloud client is vendored, not pip-installed — see
# ev_charge/vendor/audiconnect/README.md for the one-time copy step. The previous PyPI library
# (audiconnectpy) was deleted by its author, so we use the maintained Home Assistant client.
