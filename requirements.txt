# Core runtime dependencies. These always install cleanly.
requests>=2.31
python-dotenv>=1.0
aiohttp>=3.9

# Dev / tests
pytest>=8.0

# NOTE: the Audi cloud client (audiconnectpy) is intentionally NOT listed here.
# It is unofficial and frequently not installable from PyPI for a given Python version
# (pip then aborts this whole file with "from versions: none", taking the deps above with it).
# Install it separately — see requirements-audi.txt and the README. The battery field detector
# in ev_charge/audi.py tolerates API differences across audiconnectpy versions.
